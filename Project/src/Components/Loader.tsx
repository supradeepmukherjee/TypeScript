import { CircularProgress } from "@mui/material"

const Loader = () =>
  <CircularProgress
    size='30vh'
    sx={{
      ml: '47vw',
      textAlign: 'center',
      mt: '10vh'
    }}
  />

export default Loader